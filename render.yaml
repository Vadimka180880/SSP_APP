services:
  - type: web
    name: SSP_APP
    env: python
    buildCommand: |
      pip install gunicorn
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    autoDeploy: true
